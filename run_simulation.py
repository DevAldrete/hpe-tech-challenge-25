import logging

logging.basicConfig(level=logging.INFO)

if __name__ == "__main__":
    print("Starting simulation to gather data...")
    # This will simulate vehicles, but we also need the orchestrator to ingest and store the data
